import styled from "styled-components";

export const StyledModalPopUp = styled.div`
  top: 0;
  left: 0;
  z-index: 3;
  width: 100%;
  height: 100vh;
  padding: 1rem;
  display: flex;
  position: fixed;
  align-items: center;
  justify-content: center;
  background-color: rgba(0, 0, 0, 0.5);
`;
